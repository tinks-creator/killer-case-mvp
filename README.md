# Blackthorn House MVP

Static browser prototype.

Run locally:
`python3 -m http.server 8080`

Then open `http://localhost:8080`.

For iPhone testing, deploy the contents of this folder to a static host such as GitHub Pages, Netlify, or Vercel.
